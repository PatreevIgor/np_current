<%= form_for(@candies_product) do |f| %>
  <% if @candies_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candies_product.errors.count, "error") %> prohibited this candies_product from being saved:</h2>

      <ul>
      <% @candies_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :candy_id %><br>
    <%= f.number_field :candy_id %>
  </div>
  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
